#!/usr/bin/env python
import sys
import os

# Add src to Python path so example_app can be imported
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'src'))

from example_app.cli import cli

if __name__ == "__main__":
    cli()